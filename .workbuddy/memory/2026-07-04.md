## 2026-07-04

- 修复 `waitForGitHubDeployment()` 部署状态轮询：兼容 `success` / `succeeded` / `built` / `active` 等多种成功状态，增加日志输出；API 超时后新增直接轮询 CDN/Raw 文件 URL 的兜底机制。
- 视频/图片逐个上传时把文件路径传给 `waitForGitHubDeployment()`，便于兜底检测。
- 版本号本地更新为 v3.5（topbar + compliance-warning 两处）。
- 云端同步改为分项可选：新增「同步产品数据」（青绿按钮）和「同步促销配置」（金色按钮），保留「全部同步」（灰色底栏）做兜底。新增 `syncProducts()` / `syncPromo()` / `syncSingle()` 三个函数。
- 媒体管理的提示文字同步更新为指向「同步产品数据」。
- ✅ 经用户授权，v3.6 已推送至 GitHub（commit 33ecc43）。
- v3.6: fetchRemoteJSON 在 GitHub Pages 环境改用同域路径，避免 raw.githubusercontent.com 缓存延迟导致旧数据覆盖；页面启动先用 localStorage 快速渲染，远程加载完成后覆盖更新。
- 补加「媒体配置」按钮（绿色），与"产品数据""促销配置"构成三道同步入口，全部共用 `syncSingle` 逻辑。
- 把「媒体配置」按钮的图标统一为 ☁，与产品数据、促销配置保持一致。
- ✅ v3.6 首次部署（run #117）因 GitHub Pages deploy 步骤失败，重新触发 run #118 后部署成功，线上版本已更新为 v3.6。
- 视频/图片上传成功后，进度区新增「☁ 立即同步媒体配置到云端」按钮，上传完即可一键推送 pod-config.json，不用切到系统设置，避免切页面时远程数据覆盖本地。
- 云端同步按钮从各管理页底部移到管理面板工具栏，与「新增产品/新增活动」并列，随当前 tab 自动切换：产品管理(青绿)/媒体管理(绿色)/促销管理(金色)。系统设置简化为仅 Token 输入。
- 上传流程重构：批量上传成功后先等待 GitHub Pages 部署，部署完成后才显示「立即同步」按钮。等待期间实时显示轮询状态（API检测/N次重试/HEAD直连），不再死等。
- 网络公开检索核实 生花好事 评价：确认核心风味为焦糖+坚果豆香+蜂蜜，成分含香荚兰豆酊（香草奶油来源），但击喉感为强而非顺滑。
- 本地新增 生花好事（id:114）到 `disposable-config.json` 与 `index.html` 内联 `DISPOSABLE_SECTIONS`，价格 ¥129，标签「新品上市/解瘾强/焦糖坚果/12ml大容量」，描述「焦糖坚果豆香 · 强击喉 奶油蜂蜜香」。
- 修正 `getPuffs()`：id 114 返回 4000 口。
- 版本号从 v3.6 同步更新为 v3.7（topbar + compliance-warning + 注释三处）。
- 新增本地预览支持：`fetchRemoteJSON()` 在 localhost/127.0.0.1/空 hostname 时加载本地 JSON；新增 `localAssetUrl()` 将 CDN 资源路径自动转为本地文件名，便于未推送前查看图片/视频。
- 将 typo 视频文件名「花生好事.mp4」修正为「生花好事.mp4」。
- 🐛 修复：`localAssetUrl()` 初始实现把所有产品 img src 直接替换为本地路径，导致已有产品图片全部 404。改为 CDN URL 作为主 src，`onerror` 时兜底到本地文件（5 处渲染 + openVideoPlayer）。验证通过，所有 `localAssetUrl` 现在仅出现在 onerror/func 定义中。
- 修改 RELX 一次性区大标题：去掉 9.6ml（不再通用），改为纯"悦刻 RELX"；i18n 三语同步更新。
- 满园茵茵/金光奕奕/飞马环游 各补 9.6ml blue 标签。
- 生花好事售价 129 → 149。
- 媒体素材（生花好事.png, 生花好事.mp4）复制到 `extracted_images/disposable_images/` 和 `videos/`。
- ✅ v3.7 已推送至 GitHub（commit a381ea6 → 4067f21），含 5 个文件。GitHub Pages 预计 1-3 分钟部署完成。
